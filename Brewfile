# Brewfile — macOS dependencies for dotfiles
# Usage: cd ~/.dotfiles && brew bundle --file=Brewfile

# Core
brew "stow"
brew "git"
brew "git-crypt"
brew "gnupg"
brew "gh"

# Terminal tools
brew "starship"
brew "fzf"
brew "ripgrep"
brew "fd"
brew "bat"
brew "glow"
brew "lf"
brew "tmux"
brew "tree"

# Editor (needs tree-sitter for TS parser compilation)
brew "neovim"
brew "tree-sitter"

# GNU coreutils for scripting compatibility
brew "coreutils"

# fzf shell integration (handled by formula post-install)
# Run after install: $(brew --prefix)/opt/fzf/install
